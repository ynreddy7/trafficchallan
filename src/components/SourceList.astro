---
interface Props { sources: string[] }
const { sources } = Astro.props;
---
<section class="sources">
  <h2>Sources</h2>
  <ul>{sources.map((s) => <li><a class="source-host" href={s} rel="noopener nofollow">{new URL(s).hostname}</a> — {s}</li>)}</ul>
</section>
