# Upgrade ledger (overnight 13-Aug)
Chunk A: complete (9a85664), review Needs-fixes -> 1 Important (footer-nav 32px < 44px floor) FOLDED INTO CHUNK C; minors: crumbs/guide-list tap height (accepted, GOV.UK precedent), PlateBadge yellow/green variants unbuilt (no content needs them), dead tokens --focus-ink/--on-danger (cleanup in C). 9 disclosed deviations all accepted. .superpowers/upgrade now tracked in git = accepted provenance.
Chunk B: complete (pending commit). Vehicle-number/state portal finder (homepage island, src/lib/portal-finder.ts, TDD'd, all 3 result branches — state/unknown-code/invalid — verified live in-browser); calculator multi-offence checkboxes + totalFines() (honest null on range/court-decided text, TDD'd) + ?o=&s=&r= URL params + Copy link + static "Every fine at a glance" FineTable fallback; /compare/ 10-offence x 10-override-state matrix with sticky first column, linked from homepage + fines hub; FineTable `compact` prop (Offence + First offence only), used on homepage. New tokens-only CSS (.chip, .finder-*, .compare-table sticky) — no new colors/fonts. Full verification green: gate, 74 vitest tests (23 new), tsc --noEmit, build (38 pages incl. /compare/), check:links (1291 links, 0 broken); manual curl + live browser pass on all three islands, no console errors. Deliberately left computeFine's override-ignores-repeat behaviour and calculator.astro's missing overrideSource render untouched — both are pre-existing behaviours outside this chunk's brief, not introduced or worsened here.
Chunk B: complete (02df4f0, 74/74). Review Needs-fixes -> 2 Important QUEUED behind Chunk C (same-file conflict): (1) firstClause fails on 32/56 real override cells — expand delimiters + 90-char word-boundary cap, test against FULL cell population; (2) leadingAmount misses "/"-delimited dual amounts — treat "/" after figure as range marker. Minors: add AN/LD/DD to known codes; portal-finder form no-JS GET dead end (drop input name attr). Verified clean: XSS-safe rendering, URL round-trip, sticky column, compact prop, no keyword collision.
Chunk C: complete (pending commit). webPageJsonLd() in seo.ts (datePublished fixed at site-launch 2026-08-13, dateModified/lastReviewed mirror last_verified, isPartOf WebSite) wired via new Base.astro `dateModified` prop into every state/guide/offence/hub/trust page; sitemap serialize() in astro.config.mjs builds a slug->last_verified map from data/states+data/fines+guide frontmatter (gray-matter) at config load, all 37 sitemap urls now carry <lastmod>, unmapped urls fall back to no lastmod (untriggered here — every current url resolved). Pagefind wired: devDep, `pagefind --site dist` appended to build, data-pagefind-body on <main> + data-pagefind-ignore on the disclaimer (kills the footer-noise-matches-everything bug flagged in research), modular header search button + <dialog> lazy-importing /pagefind/pagefind.js only on first open (confirmed 404-in-dev -> graceful "Search available on the live site" message, 200-in-preview -> real index), '/' focus shortcut, native Escape-close. New /api/fines.json, /api/states.json (both CC-BY-licensed, updated=max last_verified), /api/fines.csv (RFC4180-ish escaping, TDD'd in src/lib/csv.ts) wired into fines/index.astro's Dataset via a new optional `distributions` param on datasetJsonLd() (existing 3-arg callers untouched) + linked from its prose and from a new llms.txt `## Data` section. rehype-slug added for markdown guides (redundant with Astro's built-in heading-id plugin but explicitly requested — confirmed no duplicate-id fallout) + slugify() (new, TDD'd in format.ts) giving h2 ids to [slug].astro's state branch and fines/[offence].astro. Small fixes: .footer-nav a now 44px (closes Chunk A's queued Important finding), --focus-ink/--on-danger dead tokens removed, html lang="en-IN". Deliberately OUT of scope and left untouched: Chunk B's queued firstClause/leadingAmount Important fixes above — my brief was C1-C4 (search/dates/API plumbing) only, and those two touch src/lib/format.ts and src/lib/fine-calc.ts logic I did not otherwise modify; flagging here so they aren't lost. Full verification green: gate, 86 vitest tests (12 new: 4 slugify, 5 csv, 3 seo distribution/webPage), tsc --noEmit, build (38 pages, pagefind indexed 38/3470 words, "Ignoring pages without this tag" confirms the ignore-list fix took), check:links (1294 links, 0 broken); manual curl against both `astro dev` (pagefind.js 404s, exercising the fallback path) and `astro preview` (pagefind.js 200s, search button HTML present) — both torn down after verification.
Chunk C: complete (532d291, 86/86, lastmod+pagefind+API all verified). Review Needs-fixes -> 1 Important QUEUED (hub dateModified scope: fines/index+calculator+compare must compute newest across states+fines+guides via one shared helper, matching buildLastmodMap; /fines/ live mismatch 08-12 vs 08-13). Minors deferred: dup launch-date constants; lastmod lacks calendar-validity check (isoDate regex loose); dialog a11y inferred-not-observed; '/' shortcut vs <select> inert quirk.
Chunk B fix round: complete (8cad3d4, 96/96 incl. whole-population firstClause test over all 56 cells; AN/LD/DD codes; no-JS form fix). Clean concurrency with Chunk D confirmed.
Chunk D: complete (78b7601, 100/100; 5 verbatim statute quotes live-verified vs India Code AJAX endpoint; question H2s; caution callouts; publish.ts consistency).
Final-fix (dates): complete (7ccdd55, 102/102, JSON-LD dateModified == sitemap lastmod verified on all 4 hub pages).
FINAL REVIEW (fable): SHIP. 0 Critical, 1 Important (datePublished 08-13 postdates dateModified 08-12 on 22/23 pages — clamp folding into current fix round), minors: dup date constants, double gate in publish (harmless), '/' modifier keys.
CONTROLLER VISUAL PASS found 2 Criticals the reviews missed: (1) search dialog visible-by-default (display:flex overrides dialog:not([open]) — fix in flight); (2) mobile 390px horizontal overflow from long unbreakable portal URLs (queued to same fixer).
